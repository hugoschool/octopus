[Unit]
Description=Poll
After=network.target

[Service]
ExecStart=/app/poll/.venv/bin/flask run --host=0.0.0.0 --port=80
KillMode=control-group
WorkingDirectory=/app/poll
Restart=on-failure

Environment="REDIS_HOST={{ hostvars['redis-1']['ansible_host'] }}"

[Install]
WantedBy=multi-user.target
